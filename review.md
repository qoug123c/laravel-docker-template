# Laravel Lesson レビュー①
## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
SELECT * FROM todos;<br>

### Todoモデルのallメソッドの返り値は何か
todosテーブルから全てのレコードを取得する

### 配列の代わりにCollectionクラスを使用するメリットは
CollectionクラスはLaravelで用意されているクラスで配列操作に特化したクラスになります。<br>
Collectionクラスはすでにインスタンス化されており、Collectionインスタンスの中にTodoインスタンスがあり、Todoインスタンスはtodosテーブルのレコードを1つずつに対応しているため、レコード情報をTodoインスタンスという形で取得できます。<br>
さらに「配列にできて、Collectionにできないことはない」ため、インスタンスをForeachなど使用することも可能です。

### view関数の第1・第2引数の指定と何をしているか
view関数の引数には表示させたいBladeファイルを指定する必要がある。`resources/views/`から対象の.blade.phpまでの相対パスを`.(ドット)`区切りで指定する。
- 第1引数：`resources/views/`以降の階層名
- 第2引数：.blade.phpとついている対象のファイル名(拡張子不要)

### index.blade.phpの$todos・$todoに代入されているものは何か
- `$todos`：todosテーブルのレコード全部(オブジェクト)
- `$todo`：todosテーブルのインデックスに該当する1つのレコード

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
新規作成ページで入力された全ての入力データを1つの連想配列にしている。

### fillメソッドは何をしているか
dbへの保存機能はないものの、db保存前に入力されたデータを連想配列で取得し、取得した値を一括にまとめてセットする。今回はTodoインスタンスの各プロパティに一括で代入します。fillメソッドを使用しない場合、それぞれの配列(連想配列)に個別で入力されたデータをセットする必要がある。
```
$user = [
  name: 'bob';
  age: '33'
];

$user -> name = $data['name'];
$user -> age = $data['age'];

↓↓ //fillを使用することでまとめられる。

$user->fill($data);
```
### $fillableは何のために設定しているか
LaravelのModel（モデル）クラス内で定義する「一括保存（Mass Assignment）を許可するカラム（項目）のリスト」です。
保存を許可するホワイトリストとなり、`$request->all()`として一括取得したデータ内に入力項目を増やしたり、画面にはない is_admin: 1（管理者権限フラグ）というデータを勝手に追加して送信したり、悪意あるデータに対して入力を無視して一括代入のリスクを防ぎます。

### saveメソッドで実行しているSQLは何か
新しくデータを作る時：insert文<br>
既存のデータを書き換えて保存する時：update文

### redirect()->route()は何をしているか
routes/web.phpでルート定義し、routeメソッド内に記述されている`.(ドット)`で区切ったbladeを省略したファイルにリダイレクトするようにしている。

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
  1. **DBの「変更履歴（バージョン管理）」が残る**<br>
    ・「いつ、誰が、どんなカラムを追加・変更したのか」がマイグレーションファイルとして歴史のように残ります。<br>
    ・履歴確認：ファイル名に日付が含まれるため、DBの変更が一目でわかるようになります。<br>
    ・やり直し(ロールバック)：`php artisan migrate:rollback`を実行することで、直線の変更を取り消して一つ前の状態に安全に戻せます。
  2. **チーム全員のDB環境がコマンド1発で同期できる**<br>
    ・新しくプロジェクトに参加したメンバーや別のPCでも、コマンドを1回叩くだけで、全く同じ構造のテーブル群が一瞬で作成されます。
  3. **テスト環境・本番環境への反映（デプロイ）が安全かつ一瞬**<br>
    ・開発環境で作った変更を本番サーバーに反映するとき、手作業でSQLを発行してテーブルをイジるのは非常に危険です。

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
- up()：`php artisan migrate`
- down()：`php artisan migrate:rollback`

### Seederクラスの役割は何か
Seeder（シーダー）クラスは、データベースを作った直後の「空っぽの状態」のテーブルに対して、最初に必要なレコード（データ）をあらかじめ入れておく（流し込む）ために使います。

### route関数の引数・返り値・使用するメリット
- 引数
  - 第一引数(必須)：`routes/web.php`でルート定義されたルート名。
  - 第二引数(任意)：「URLの {id} などの穴を埋めるための具体的なデータ（数字や変数）」。
- 返り値
  - 型：string（文字列）
  - 返ってくる値：生成されたURL文字列
- メリット
  - 後から web.php 側のURL（/items/detail/{id}）をどのように変更しても、Blade側のコードを1箇所も修正する必要がなくなります！ route() 関数が最新の正しいURLを自動生成してくれます。

### @extends・@section・@yieldの関係性とbladeを分割するメリット
```
@extends
```
子のbladeファイルに`@extends`を記述する。各bladeファイルで共通部分を親としてbase.blade.phpファイルを作成。それぞれの子のbladeファイル共通してる部分を記述した親(base.blade.php)を呼び出す。
```
@section
```
子のbladeファイルに記述する。親ファイルの共通部分以外を`@section~@endsection`で囲う。必ず`@section`の後に`('content')`などキーを指定する
```
@yield
```
親のbladeファイルに記述する。子のbladeファイルにて`@section~@endsection`で囲った同じキー箇所を呼び出しする。これによって1枚のbladeファイルを生成する。

### @csrfは何のための記述か
「悪意のある第3者による勝手なフォーム送信（なりすまし攻撃）」CSRF攻撃を防ぐセキュリティ対策のため。

### {{ }}とは何の省略系か
- PHPの echo 出力
- エスケープ処理
