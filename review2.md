# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
todosテーブルのidカラム

### findメソッドで実行しているSQLは何か
select * from todos where id = {$id};

### findメソッドで取得できる値は何か
findメソッドの引数：該当テーブルのプライマリーキー<br>
- 該当のプライマリキーを持つ1件のレコード全体が、モデルオブジェクトとして取得できます。
- 該当プライマリーキーのレコードがない場合はnullを返します。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
遷移してきたページ
createページから遷移してきた場合：INSERT文を実行<br>
  - Routingファイル(routes/web.php)にて記述されている`Route::post`の記述を見て判別している。
editページから遷移してきた場合：UPDATE文を実行<br>
  - Routingファイル(routes/web.php)にて記述されている`Route::put`の記述を見て判別している。<br>

| ルーティング | 役割 | 主な利用シーン |
| :--- | --- | --- |
| `Route::get` | データの画面取得 | ページを表示する、詳細を見る |
| `Route::post` | 新しいデータの追加 | 会員登録、新規投稿の送信 |
| `Route::put` | 既存データの上書き更新 | プロフィールの変更、投稿記事の編集 |
| `Route::delete` | データの削除 | アカウント削除、投稿削除 |


## Todo論理削除

### traitとclassの違いとは
  #### **class(クラス)**
  オブジェクト指向の基本単位です。「データ（属性）」と「それに対する操作（メソッド）」をセットにして管理し、プログラム中で実際に動くオブジェクト（インスタンス）を生成します。<br>
  親クラスの機能を子クラスが引き継ぐ「継承」が可能ですが、多くの言語（Java、PHP、Scala、Rustなど）では、複雑化や衝突を避けるために「1つのクラスは1つの親クラスからしか継承できない（単一継承）」というルールがあります。
  #### **trait(トレイト)**
  クラスにプロパティやメソッドを追加するための機能です。<br>
  複数のクラス間で「同じ処理・挙動」を使い回したいけれど、共通の親クラスを持たせるのが不自然な場合に便利です。

| 項目 | class(クラス) | trait(トレイト) |
| ---- | ---- | ---- |
| 単体での実体化 | できる(newなどで生成) | できない (単体でインスタンス化不可) |
| 主な役割 | オブジェクトの構造や状態（データ）の定義 | 共通機能・挙動（メソッド）の部品化・共有 |
| 多重継承/挿入 | 単一継承のみの言語が多い (Java, PHPなど) | 複数のトレイトを1つのクラスに組み込める |
| 状態（フィールド） | 独自のデータ（変数）を保持・管理する | 原則として処理（メソッド）のみ（言語により異なる） |


### traitを使用するメリットとは
複数のクラス間でのコードの共通化。再利用することが可能になります。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
```
class TodoController extends Controller
{
    private $todo;

    public function __construct(Todo $todo)
    {
        $this->todo = $todo;
    }
```
上記はclass直下に`private $todo;`の記述があります。これはTodoController内どこでも呼び出し可能なクラスプロパティです。<br>
合わせて、constructメソッド内、` $this->todo = $todo;`の記述でコンストラクタインジェクションで生成したTodoクラスのインスタンスをプロパティに代入しています。

よって、ユーザーがWebサイトの特定のURLにアクセスした瞬間にコンストラクタは実行されます。

### RequestクラスからFormRequestクラスに変更した理由
バリデーションを行うため。<br>
FormRequestクラスは、バリデーションを行うクラスの基本となるクラスです。

### $errorsのhasメソッドの引数・返り値は何か
- 引数：文字列型。入力欄のname属性
- 返り値；bool型。その入力欄でバリデーションエラーが発生しているか真偽。
  - true: 指定した name 属性に関するバリデーションエラーメッセージが存在する場合
  - false: エラーメッセージが存在しない（正常に入力されている）場合

### $errorsのfirstメソッドの引数・返り値は何か
- 引数：
  - 第一引数(必須(key))：文字列型もしくはNULL型。入力欄のname属性。
  - 第二引数(任意)：文字列型もしくはNULL型(デフォルトはNULL)。出力されるメッセージのフォーマット指定。
- 返り値；文字列型。指定したname属性に対応する最初のエラーメッセージ1件を文字列で返します。

### フレームワークとは何か
システムやアプリを開発する際に、良く使うコードをパッケージ化したもののことを指します。ゼロからコードを書く必要がなく、開発速度や品質が飛躍的に向上します。

### MVCはどういったアーキテクチャか
MVC（Model-View-Controller）は、アプリやWebシステムのプログラムを「役割ごとに3つのコンポーネントに分離する」プログラミングの設計手法（アーキテクチャ）です。
| MVC | 役割 |
| :----: | ---- |
| Model | DBとのやりとりを司る。 |
| View | ユーザー(Client)に表示する画面の生成を司る。 |
| Controller | ユーザーからの入力(送信データ)に基づき、Model・Viewの制御を司る。 |
| Routing | ユーザーからの依頼(リクエスト)に応じてどの処理を動かすかの判断を司る。 |

### ORMとは何か、またLaravelが使用しているORMは何か
#### ORMとは
- 「**Object-Relational Mapping**：オブジェクト関係マッピング」の略。
- ORMとは、プログラミング言語のClassとデータベースのテーブルをマッピング（関連付け）することでSQLを直接操作することなくデータベースとマッピングされたClassのメソッドを用いることでDBとやり取りを行うことができます。
#### Laravelが使用しているORMは何か
- Eloquent

### composer.json, composer.lockとは何か
PHPのパッケージ管理ツール Composer が使用する設定ファイルです。
| ファイル名 | 役割 | 主な特徴 | 主な特徴 |
| :----: | ---- | ---- | ---- |
| `composer.json` | **希望するライブラリの定義** | 必要なライブラリ名とバージョン範囲を指定 | **必須** |
| `composer.lock` | 実際にインストールされた構成の固定 | 実際にインストールされた正確なバージョンやハッシュ値を記録 | **必須** |

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
LaravelでComposerを使用してインストールしたパッケージ（ライブラリ）は、すべてプロジェクトのルート直下にある **vendor ディレクトリ**に格納されます。